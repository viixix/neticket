name: CD - Deploy to Production

on:
  workflow_dispatch:

jobs:
  detect-changes:
    name: Detect Changed Services
    runs-on: ubuntu-latest
    outputs:
      has_changes: ${{ steps.detect.outputs.has_changes }}
      changed_services: ${{ steps.detect.outputs.changed_services }}
      frontend_changed: ${{ steps.detect.outputs.frontend_changed }}
      show_changed: ${{ steps.detect.outputs.show_changed }}
      booking_changed: ${{ steps.detect.outputs.booking_changed }}
      queue_changed: ${{ steps.detect.outputs.queue_changed }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect changes
        id: detect
        run: |
          chmod +x .github/scripts/detect-changes.sh
          # Compare with previous commit on main
          .github/scripts/detect-changes.sh HEAD~1

  # Frontend Deploy
  deploy-frontend:
    name: Deploy - Frontend
    needs: detect-changes
    if: needs.detect-changes.outputs.frontend_changed == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to frontend server
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SERVER_HOST: ${{ secrets.FRONTEND_SERVER_HOST }}
          SERVER_USER: ${{ secrets.FRONTEND_SERVER_USER }}
          GCP_REGISTRY: ${{ secrets.GCP_REGISTRY }}
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
          
          IMAGE_TAG: ${{ github.sha }}
        run: |
          # SSH 키 설정
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H $SERVER_HOST >> ~/.ssh/known_hosts

          # 프론트엔드 서버에 이미지 pull 및 배포
          ssh -i ~/.ssh/deploy_key $SERVER_USER@$SERVER_HOST << EOF
            # GCP Artifact Registry 로그인
            echo "$GCP_SA_KEY" | docker login -u _json_key --password-stdin "https://$(echo $GCP_REGISTRY | cut -d/ -f1)"

            # CI에서 빌드된 이미지 pull
            docker pull $GCP_REGISTRY/neticket-frontend:$IMAGE_TAG

            # 기존 컨테이너 중지 및 삭제
            docker stop frontend 2>/dev/null || true
            docker rm frontend 2>/dev/null || true

            # 새 컨테이너 실행
            docker run -d \
              --name frontend \
              -p 80:3000 \
              --restart unless-stopped \
              -e NODE_ENV=production \
              -e NEXT_PUBLIC_API_URL=http://localhost/api \
              -e NEXT_PUBLIC_API_MODE=mock \
              $GCP_REGISTRY/neticket-frontend:$IMAGE_TAG

            # 이전 이미지 정리 (현재 사용 중인 이미지 제외)
            docker image prune -f

            echo "✅ Frontend deployed successfully from registry (image built in CI)"
          EOF

  # Backend Servers Deploy (show + booking)
  deploy-backend:
    name: Deploy - Backend Servers
    needs: detect-changes
    if: needs.detect-changes.outputs.show_changed == 'true' || needs.detect-changes.outputs.booking_changed == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to backend server
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SERVER_HOST: ${{ secrets.BACKEND_SERVER_HOST }}
          SERVER_USER: ${{ secrets.BACKEND_SERVER_USER }}
          SHOW_CHANGED: ${{ needs.detect-changes.outputs.show_changed }}
          BOOKING_CHANGED: ${{ needs.detect-changes.outputs.booking_changed }}
        run: |
          # SSH 키 설정
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H $SERVER_HOST >> ~/.ssh/known_hosts

          # 백엔드 서버에서 빌드 및 배포
          ssh -i ~/.ssh/deploy_key $SERVER_USER@$SERVER_HOST << EOF
            cd /app/neticket

            # 최신 코드 가져오기
            git fetch origin main
            git reset --hard origin/main

            cd backend

            # 변경된 서비스만 재배포
            if [ "$SHOW_CHANGED" == "true" ]; then
              echo "🚀 Deploying show..."
              docker-compose build show
              docker-compose up -d show
              echo "✅ show deployed successfully"
            fi

            if [ "$BOOKING_CHANGED" == "true" ]; then
              echo "🚀 Deploying booking..."
              docker-compose build booking
              docker-compose up -d booking
              echo "✅ booking deployed successfully"
            fi

            # 이전 이미지 정리
            docker image prune -f
          EOF

  # Queue Backend Deploy
  deploy-queue:
    name: Deploy - Queue Backend
    needs: detect-changes
    if: needs.detect-changes.outputs.queue_changed == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to queue server
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SERVER_HOST: ${{ secrets.QUEUE_SERVER_HOST }}
          SERVER_USER: ${{ secrets.QUEUE_SERVER_USER }}
        run: |
          # SSH 키 설정
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H $SERVER_HOST >> ~/.ssh/known_hosts

          # 큐 서버에서 빌드 및 배포
          ssh -i ~/.ssh/deploy_key $SERVER_USER@$SERVER_HOST << 'EOF'
            cd /app/neticket

            # 최신 코드 가져오기
            git fetch origin main
            git reset --hard origin/main

            # queue 서비스 재배포
            cd queue
            docker-compose build
            docker-compose up -d

            # 이전 이미지 정리
            docker image prune -f

            echo "✅ Queue Backend deployed successfully"
          EOF

  # Deployment Summary
  deployment-summary:
    name: Deployment Summary
    needs: [detect-changes, deploy-frontend, deploy-backend, deploy-queue]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Check Deployment Results
        run: |
          echo "=== Deployment Summary ==="
          echo "Changed services: ${{ needs.detect-changes.outputs.changed_services }}"

          if [ "${{ needs.detect-changes.outputs.has_changes }}" == "false" ]; then
            echo "No services changed - skipping deployment"
            exit 0
          fi

          echo ""
          echo "Deployment Results:"
          echo "- Frontend: ${{ needs.deploy-frontend.result }}"
          echo "- Backend (API + Ticket): ${{ needs.deploy-backend.result }}"
          echo "- Queue Backend: ${{ needs.deploy-queue.result }}"

          # Check if any deployment failed
          if [ "${{ contains(needs.*.result, 'failure') }}" == "true" ]; then
            echo ""
            echo "❌ Some deployments failed"
            exit 1
          fi

          echo ""
          echo "✅ All deployments completed successfully"
